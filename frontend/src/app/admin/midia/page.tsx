import { MediaManager } from "@/components/admin/MediaManager";

export default function MediaAdminPage() {
  return <><header className="admin-page-header"><div><span className="eyebrow">Otimização automática</span><h1>Gestor de mídia</h1><p>Imagens WebP e vídeos H.264 prontos para redes móveis.</p></div></header><MediaManager /></>;
}
